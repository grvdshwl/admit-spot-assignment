import { Container, Typography } from "@mui/material";
import styled from "styled-components";

export const ExpenseTrackerContainer = styled(Container)``;

export const ExpenseTrackerTitle = styled(Typography)``;
